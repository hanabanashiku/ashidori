import React from "react";
import Layout from "./layout";
import SignIn from "./SignIn";

const Options = () => {
  return (
    <Layout>
      <SignIn />
    </Layout>
  );
};

export default Options;
